name: Governance Rules Validation
description: |
  Verify each governance rule behaves distinctly.
  Different rules should produce different pass rates.
baseConfig:
  template: compound
  overrides:
    voting_activity: 0.3
  population:
    totalMembers: 90
sweep:
  parameter: governance_rule
  values:
    - majority
    - quorum
    - supermajority
execution:
  runsPerConfig: 20
  stepsPerRun: 300
  seedStrategy: sequential
  baseSeed: 42
metrics:
  - name: Proposal Pass Rate
    type: builtin
    builtin: proposal_pass_rate
  - name: Average Turnout
    type: builtin
    builtin: average_turnout
  - name: Total Proposals
    type: builtin
    builtin: total_proposals
output:
  directory: results/validation/governance-rules
  formats:
    - json
    - csv
